const state = {
  show: false
}

const getters = {}

const mutations = {
  SWITCH_RIGHT_BAR_SHOW (state, flag) {
    state.show = flag
  },
}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
